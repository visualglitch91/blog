import styled from "@emotion/styled"
import { rhythm, scale, primaryColor } from "../utils/typography"

const Card = styled.div`
  background: white;
  border-top: 16px solid ${primaryColor};
  padding: 30px;
`

export default Card
